# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# turbo
.turbo

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# Исходники видео (финалы: intro.webm, intro.mp4, intro-lite.mp4)
/public/intro-new.mp4
/public/IMG_5316 (online-video-cutter.com).mp4

# Большие видео с локалки не грузим в git (на проде заливаются через админку)
/public/photos/*.mp4
/public/photos/*.webm
/public/photos/*.mov
/public/photos/*.avi

# База SQLite — не в git (на проде хранится локально, при деплое сохраняется)
/prisma/*.db
/prisma/prisma/

# Синхронизация с продом — только для локальной разработки
/scripts/sync-from-prod.mjs
